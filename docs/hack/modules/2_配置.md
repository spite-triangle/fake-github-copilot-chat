# 配置

## tsconfig.base

- 文件：`tsconfig.base.json`
- 修改

```json
{
    "compilerOptions": {
		/* .. */
		"noUnusedLocals": false,
		"noUnusedParameters": false,
		/* ... */
    }
}
```



## 删除工作流程

删除 `.github\workflows` 下所有的 `*.yml` 配置